/*.then(data => fetch(
  '/games/uncovered/'+ data.roomID + '/join', {
    method: 'POST',
    body: JSON.stringify()
  }
))
*/
import React from 'react';

export default () => {
  return (<div>Hello World</div>);
}
